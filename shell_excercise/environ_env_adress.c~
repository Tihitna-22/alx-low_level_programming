#include <stdio.h>

/**
 * main - prints the adress of env and environ
 
 * Return: Always 0.
 */
extern char **environ;

int main(int ac, char **av, char **env)
{
/**
 *printf("address of env:%u \n ",(unsigned int)&env);
 * printf("address of environ:%u \n",(unsigned int)&environ);
*/

  printf("Address of env: %lu\n", (unsigned long)&env);
  printf("Address of environ:%lu\n", (unsigned long)&environ);
  return 0;
}
